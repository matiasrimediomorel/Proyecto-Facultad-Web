// Configuración base de la API
// EDITA apiBase con la URL donde corre tu backend (incluye /api al final).
// Ej: 'http://localhost:57085/api' o 'https://mi-backend.com/api'
window.API_CONFIG = {
  apiBase: 'https://localhost:44375/api', // <-- AJUSTA A TU ENTORNO
  // Si usás credenciales por cookie/sesión, pon credentials: 'include'
  // Si no, mantenelo como 'same-origin' o 'omit'
  credentials: 'same-origin'
};